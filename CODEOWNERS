# CODEOWNERS — Vector
#
# CODEOWNERS lists GitHub identities that must review changes to a path.
# Vector engineers (FiveMDev, ServerInfra) are Paperclip agents and do not
# yet have GitHub identities, so today every path is owned by the CTO.
#
# When an engineer is provisioned a GitHub identity (or we create a GH team
# for them), replace the placeholders below. Until then the structure stays
# documented but inactive — do not add agent names that GitHub can't resolve.

* @vizuugit

# Server scripting / gameplay code → FiveMDev (gameplay engineer).
#   /resources/   @<fivem-dev-gh>
#   /server/      @vizuugit @<fivem-dev-gh>
#   /db/          @<fivem-dev-gh>

# Infra, deploy, CI, anti-cheat → ServerInfra (infrastructure engineer).
#   /ops/         @<server-infra-gh>
#   /.github/     @<server-infra-gh>
#   /server/server.cfg* @<server-infra-gh>

# ADRs are CTO-signoff regardless of fan-out.
/docs/adr/      @vizuugit
